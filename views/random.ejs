<%- include('partials/head') %> 

<%- include('partials/navbar') %> 
    <h1>Your random number is: <%= num %></h1>
    <% if (num % 2 === 0) { %> 
        <h2> This is an Even number</h2>
    <% }else { %> 
        <h2> This is an Odd number</h2>
    <% } %>
    <h3> its <%= num % 2===0 ? 'EVEN' : 'ODD' %> </h3>

    <%- include('partials/footer') %>